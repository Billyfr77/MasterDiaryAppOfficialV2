{
  "name": "masterdiaryappofficialv2",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "npm start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "billy": "file:..",
    "d3": "^7.9.0",
    "socket.io-client": "^4.8.1"
  }
}
